# JSON с контентом сайта — только через PHP (админка), не напрямую из браузера
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
